--------------------------------------------------------
--  DDL for Type REC_SALDO_STK_COMPROMETIDO
--------------------------------------------------------

  CREATE OR REPLACE NONEDITIONABLE TYPE "USR_TSI_SUITE"."REC_SALDO_STK_COMPROMETIDO" AS OBJECT (
    saldo numeric(16,4),
    saldo_alm numeric(16,4)
);

/
