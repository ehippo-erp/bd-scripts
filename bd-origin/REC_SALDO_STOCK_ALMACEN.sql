--------------------------------------------------------
--  DDL for Type REC_SALDO_STOCK_ALMACEN
--------------------------------------------------------

  CREATE OR REPLACE NONEDITIONABLE TYPE "USR_TSI_SUITE"."REC_SALDO_STOCK_ALMACEN" AS OBJECT(
  SALDO NUMERIC(16, 4),
  SALDO_ORI NUMERIC(16, 4)
  );

/
